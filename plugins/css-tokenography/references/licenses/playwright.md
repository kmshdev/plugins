# Playwright license record

- Source: <https://github.com/microsoft/playwright>
- Pinned package: `@playwright/test@1.62.0`
- Declared license: `Apache-2.0`
- Adoption: `optional-dev-adapter`

Playwright provides deterministic browser collection in the optional laboratory.
The plugin never installs browser binaries implicitly. Local and CI setup must
run the documented Playwright install command explicitly.

`npm audit --json` on 2026-07-30 reported no findings in the pinned browser
workspace.
